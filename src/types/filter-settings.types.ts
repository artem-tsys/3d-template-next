export type FilterSettings = {
	BUILDINGS_PRESETS: string[];
	ROOM_PRESETS: number[];
	AREA_MIN: number;
	AREA_MAX: number;
	FLOOR_FROM: number;
	FLOOR_TO: number;
	PRICE_MIN: number;
	PRICE_MAX: number;
};
